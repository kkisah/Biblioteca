@page
@model LoginModel

@{
    ViewData["Title"] = "✧˖° Entrar";
}

<div class="container d-flex justify-content-center">
    <section id="divLogin" class="text-dark">
        <h3 class="text-center pt-3 pb-3">Bem Vindo ao Bibliotech  ༘ ⋆｡˚ </h3>
        <form id="account" method="post">
            <hr />
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="custom-alert alert alert-danger alert-dismissible fade show" role="alert">
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <p class="fw-bold">@error.ErrorMessage</p>
                    }
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }

            <div class="form-floating mb-3">
                <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                <label asp-for="Input.Email" class="form-label">Email</label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="form-floating mb-3">
                <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                <label asp-for="Input.Password" class="form-label">Senha</label>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="checkbox mb-3 checkbox-container">
                <label asp-for="Input.RememberMe" class="form-label checkbox-label">
                    <input class="form-check-input custom-checkbox p-2" asp-for="Input.RememberMe" />
                    Lembrar de Mim?
                </label>
            </div>
            <div>
                <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary">Entrar</button>
            </div>
            <div class="d-flex justify-content-between text-dark mt-3">
                <p>
                    <a class="nav-link text-dark" id="forgot-password" asp-page="./ForgotPassword">Esqueceu a senha?</a>
                </p>
                <p>
                    <a class="nav-link text-dark" asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Registrar um novo usuário</a>
                </p>
            </div>
        </form>
    </section>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
